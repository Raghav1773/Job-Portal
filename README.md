## 📌 Overview

This is the **Frontend** part of the **Job Portal** project — a web application that connects job seekers with employers. It provides a responsive user interface where users can browse jobs, 
register/login, apply for jobs, and manage personal profiles.  
The frontend is built with **React** (or your actual framework), modern CSS, and integrates with the backend API to fetch and display job data.

## 🚀 Features

- User authentication (Sign up / Login)
- Job listing and search
- Job details view
- Apply for jobs
- Responsive design for desktop & mobile
- Connects with backend API for data

## 🧰 Tech Stack

Frontend:
- **React** (or specify framework if different)
- **React Router** (for navigation)
- **Axios / Fetch API** (for backend requests)
- CSS / Tailwind / Bootstrap (specify if used)
